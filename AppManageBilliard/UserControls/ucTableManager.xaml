<UserControl x:Class="AppManageBilliard.UserControls.ucTableManager"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="#F8F9FA">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="icTableList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Margin="15" HorizontalAlignment="Left"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Width="130" Height="100" Margin="10" Cursor="Hand" Tag="{Binding}">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="brdTable" CornerRadius="10" BorderThickness="1.5">
                                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="18" HorizontalAlignment="Center"/>
                                            <TextBlock Text="{Binding Status}" FontSize="13" Margin="0,5,0,0" HorizontalAlignment="Center" Opacity="0.8"/>
                                        </StackPanel>
                                    </Border>

                                    <ControlTemplate.Triggers>
                                        <DataTrigger Binding="{Binding Status}" Value="Trống">
                                            <Setter TargetName="brdTable" Property="Background" Value="White"/>
                                            <Setter TargetName="brdTable" Property="BorderBrush" Value="#2ECC71"/>
                                            <Setter Property="Foreground" Value="#333"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="Có người">
                                            <Setter TargetName="brdTable" Property="Background" Value="#3498DB"/>
                                            <Setter TargetName="brdTable" Property="BorderBrush" Value="#2980B9"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>