<UserControl x:Class="AppManageBilliard.UserControls.ucTableManager"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="TableButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="15" Padding="10">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FilterButtonStyle" TargetType="RadioButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="bdr" Background="White" BorderBrush="#DDD" BorderThickness="1" 
                                CornerRadius="20" Padding="15,8" Margin="0,0,10,0" Cursor="Hand">
                            <ContentPresenter HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="bdr" Property="Background" Value="#007ACC"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter TargetName="bdr" Property="BorderThickness" Value="0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid Background="#F0F2F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <RadioButton Content="Tất cả" IsChecked="True" Style="{StaticResource FilterButtonStyle}"/>
            <RadioButton Content="Đang sử dụng" Style="{StaticResource FilterButtonStyle}"/>
            <RadioButton Content="Còn trống" Style="{StaticResource FilterButtonStyle}"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <WrapPanel x:Name="wpTableList" Margin="10" ItemWidth="140" ItemHeight="140">

                <Button Style="{StaticResource TableButtonStyle}" Background="#4CAF50" Tag="Trong">
                    <StackPanel>
                        <TextBlock Text="Bàn 1" FontSize="18" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                        <TextBlock Text="Trống" FontSize="12" Foreground="#E8F5E9" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource TableButtonStyle}" Background="#F44336" Tag="CoNguoi">
                    <StackPanel>
                        <TextBlock Text="Bàn 2" FontSize="18" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                        <TextBlock Text="Có người" FontSize="12" Foreground="#FFEBEE" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                        <Border Background="White" CornerRadius="10" Padding="8,2" Margin="0,5,0,0">
                            <TextBlock Text="00:30:15" FontSize="12" Foreground="Black" FontWeight="Bold"/>
                        </Border>
                    </StackPanel>
                </Button>

            </WrapPanel>
        </ScrollViewer>
    </Grid>

</UserControl>