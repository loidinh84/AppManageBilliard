<UserControl x:Class="AppManageBilliard.UserControls.ucTableManager"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="#F8F9FA">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="icTableList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Margin="15" HorizontalAlignment="Left"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Width="130" Height="150" Margin="10" Cursor="Hand" Click="btnTable_Click" Tag="{Binding}">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="brdTable" CornerRadius="12" BorderThickness="0">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.1"/>
                                        </Border.Effect>

                                        <Grid>
                                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                                <TextBlock Text="🎱" FontSize="36" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center"/>

                                                <TextBlock Text="{Binding CategoryName}" FontSize="11" Opacity="0.8" HorizontalAlignment="Center" Margin="0,4"/>

                                                <TextBlock Text="{Binding Status}" FontSize="12" HorizontalAlignment="Center"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>

                                    <ControlTemplate.Triggers>
                                        <DataTrigger Binding="{Binding Status}" Value="Trống">
                                            <Setter TargetName="brdTable" Property="Background" Value="#FFFFFF"/>
                                            <Setter TargetName="brdTable" Property="BorderBrush" Value="#2ECC71"/>
                                            <Setter TargetName="brdTable" Property="BorderThickness" Value="2"/>
                                            <Setter Property="Foreground" Value="#333"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="Có người">
                                            <Setter TargetName="brdTable" Property="Background" Value="#3498DB"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="brdTable" Property="Opacity" Value="0.8"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>